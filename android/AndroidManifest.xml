<?xml version="1.0" encoding="utf-8"?>
<!--
  Custom AndroidManifest for Arc Raiders Companion App.
  PINNED to dioxus-cli 0.7.9's generated manifest; dx uses this file VERBATIM
  ([application] android_manifest in Dioxus.toml). Only android:label differs
  from the dx default (set to the spaced display name). Re-sync against dx's
  AndroidManifest.xml.hbs when upgrading dx. The @mipmap/@style/@xml references
  below are still generated by dx into the res/ folder.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />

    <application android:hasCode="true" android:icon="@mipmap/ic_launcher"
        android:extractNativeLibs="true"
        android:allowNativeHeapPointerTagging="false"
        android:label="Arc Raiders Companion App"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity android:configChanges="orientation|screenLayout|screenSize|keyboardHidden"
            android:exported="true"
            android:label="Arc Raiders Companion App"
            android:name="dev.dioxus.main.MainActivity">
            <meta-data android:name="android.app.lib_name" android:value="main" />
            <meta-data android:name="android.app.func_name" android:value="ANativeActivity_onCreate" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
